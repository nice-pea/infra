global:
  scrape_interval: 15s  # By default, scrape targets every 15 seconds.

scrape_configs:
  # - job_name: 'prometheus'
  #   static_configs:
  #     - targets: ['localhost:9090']

  - job_name: 'node_exporter'
    # metrics_path: /metrics
    static_configs:
      # - targets: ['host.containers.internal:9560'] # for podman
      - targets: ['172.17.0.1:9560'] # for docker


